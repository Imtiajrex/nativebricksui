# Checkbox

A control that allows the user to toggle between checked and unchecked states.

## Installation

```bash
npm install @ui/components
```

## Import

```tsx
import { Checkbox } from '@ui/components';
```

## Usage

```tsx
export default function CheckboxDemo() {
  const [checked, setChecked] = useState(false);

  return <Checkbox checked={checked} onCheckedChange={setChecked} />;
}
```

## Features

- Controlled and uncontrolled modes
- Accessible WAI-ARIA checkbox
- Keyboard navigation
- Focus management
- Custom styling support
- Form integration
- Disabled state

## Props

### Root

| Prop              | Type                         | Description                         |
| ----------------- | ---------------------------- | ----------------------------------- |
| `checked`         | `boolean`                    | The controlled checked state        |
| `defaultChecked`  | `boolean`                    | The default checked state           |
| `onCheckedChange` | `(checked: boolean) => void` | Callback when checked state changes |
| `disabled`        | `boolean`                    | Whether the checkbox is disabled    |
| `required`        | `boolean`                    | Whether the checkbox is required    |
| `className`       | `string`                     | Optional class name for styling     |

## Examples

### Basic Checkbox

```tsx
<Checkbox />
```

### Controlled Checkbox

```tsx
const [checked, setChecked] = useState(false);

<Checkbox checked={checked} onCheckedChange={setChecked} />;
```

### With Label

```tsx
<View className="flex-row items-center gap-2">
  <Checkbox id="terms" />
  <Label htmlFor="terms">Accept terms and conditions</Label>
</View>
```

### Disabled State

```tsx
<Checkbox disabled />
<Checkbox disabled checked />
```

### Form Integration

```tsx
<form onSubmit={handleSubmit}>
  <View className="flex-col gap-4">
    <View className="flex-row items-center gap-2">
      <Checkbox id="marketing" name="marketing" required />
      <Label htmlFor="marketing">Receive marketing emails</Label>
    </View>
    <Button type="submit">Subscribe</Button>
  </View>
</form>
```

### Custom Styling

```tsx
<Checkbox className="h-6 w-6 rounded-full border-2" />
```
